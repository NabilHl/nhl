{% comment %}
  Merged Liquid template: Full solar system animation from template.html + style.css
{% endcomment %}

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Solar System</title>
  <link href="https://fonts.googleapis.com/css?family=Montserrat:300,400,700" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.css" />

  <style>
    {% raw %}
    {{ 'style.css' | asset_url | stylesheet_tag }}
    {% endraw %}

    /* Paste full contents of your style.css here if not using asset_url */
    /* Example below: (truncated for space, you MUST paste full contents) */
    body {
      overflow: hidden;
      background: black;
      height: 100vh;
      font-family: "Montserrat", sans-serif;
    }
    .logo {
      color: white;
      position: absolute;
      top: 40px;
      left: 0;
      right: 0;
      margin: auto;
      text-align: center;
      font-size: 14px;
      text-transform: uppercase;
      font-weight: 100;
      letter-spacing: 4px;
    }
    .logo span {
      font-size: 12px;
      color: #f39041;
      display: block;
    }

    /* ... now paste ALL of style.css here */
  </style>
</head>
<body>
  <h1 class='logo'>
    Solar explorer
    <span>in only CSS</span>
  </h1>

  <!-- Paste entire <body> contents of template.html here -->
  <input class='planet9' id='pluto' type='radio' name='planet'>
  <label class='menu pluto' for='pluto'>
    <div class='preview'></div>
    <div class='info'>
      <h2><div class='pip'></div>Pluto</h2>
      <h3>39.5 AU</h3>
    </div>
  </label>

  <input class='planet8' id='neptune' type='radio' name='planet'>
  <label class='menu neptune' for='neptune'>
    <div class='preview'></div>
    <div class='info'>
      <h2><div class='pip'></div>Neptune</h2>
      <h3>30.06 AU</h3>
    </div>
  </label>

  <!-- Continue pasting ALL remaining inputs and labels -->
  <!-- If needed, wrap inner sections in a div with class like `.solar-system-container` -->

</body>
</html>


